# Backend environment variables

# Required
# Optional (recommended): Hugging Face Inference API token (free tier available)
# HF_TOKEN=hf_your_token_here
# If you don't set HF_TOKEN, the backend will use a rule-based fallback (no API key).

# Optional: Adzuna Job Search API
ADZUNA_APP_ID=your_adzuna_app_id
ADZUNA_APP_KEY=your_adzuna_app_key
# Country code, e.g. gb, us, ae, au, ca
ADZUNA_COUNTRY=gb

# Default location text when none inferred
DEFAULT_JOB_LOCATION=Remote

# CORS origins (comma-separated). Example:
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CORS_ORIGINS=http://localhost:5173
